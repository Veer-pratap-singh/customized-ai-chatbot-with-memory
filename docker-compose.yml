version: "3.9"

services:

  chatbot:

    build: .

    container_name: ai_chatbot

    ports:
      - "8501:8501"

    restart: unless-stopped

    volumes:
      - .:/app

    env_file:
      - .env